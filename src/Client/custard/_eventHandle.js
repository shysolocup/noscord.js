const { Client } = require('../index.js');
const { Soup } = require('stews');


Client.newC("_eventHandle", function(/**/) {
    const client = this;
    const types = client.types;
    const base = client._base;
});
