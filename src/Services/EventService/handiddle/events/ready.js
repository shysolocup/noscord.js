module.exports = (handler) => { handler.init(

    
    name = "ready",  // name of the event

    
    func = async function (/**/) { // formatting for types and stuff
        return Array.from(arguments);
    }, 

    
    glue = "ready" // what discord.js event it's tied to

    
)}
