module.exports = (handler) => { handler.init(

    
    name = ["reactionRemove", "removeReaction"],  // names of the event

    
    func = function (...args) { // formatting for types and stuff
        return args;
    }, 

    
    glue = "messageReactionRemove" // what discord.js event it's tied to

  
)}
