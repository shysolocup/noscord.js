module.exports = (handler) => { handler.init(

    
    name = [ "autmod", "autoModerationActionExecution" ],  // name of the event

    
    func = async function (...args) { // formatting for types and stuff
        return args;
    }, 

    
    glue = "autoModerationActionExecution" // what discord.js event it's tied to

    
)}
