module.exports = (handler) => { handler.init(

    
    name = ["reaction", "react"],  // names of the event

    
    func = function (...args) { // formatting for types and stuff
        return args;
    }, 

    
    glue = "messageReactionAdd" // what discord.js event it's tied to

  
)}
