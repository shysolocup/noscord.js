module.exports = (handler) => { handler.init(

    
    name = [ "ruleDelete", "autoModerationRuleDelete" ],  // name of the event

    
    func = async function (...args) { // formatting for types and stuff
        return args;
    }, 

    
    glue = "autoModerationRuleDelete" // what discord.js event it's tied to

    
)}
